{
  "identifier": "settings",
  "description": "Capabilities for the Wisp settings window",
  "windows": ["settings"],
  "permissions": [
    "core:default",
    "core:event:default",
    "store:allow-load",
    "store:allow-get",
    "store:allow-set",
    "store:allow-save"
  ]
}
