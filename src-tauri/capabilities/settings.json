{
  "identifier": "settings",
  "description": "Capabilities for the Polter settings window",
  "windows": ["settings"],
  "permissions": [
    "core:default",
    "core:event:default",
    "store:allow-load",
    "store:allow-get",
    "store:allow-set",
    "store:allow-save"
  ]
}
